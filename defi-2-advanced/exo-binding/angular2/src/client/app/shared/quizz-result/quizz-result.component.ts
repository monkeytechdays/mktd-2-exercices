import { Component, Input } from '@angular/core';
import { QuizzResult} from '../quizz/quizz.model';

// TODO Créer le composant, il faut un attribute result du type QuizzResult
// il faut ajouter le décorateur (moduleId, selector, templateUrl, styleUrls)
// il faut un attribute result du type QuizzResult
// Ce composant est utiliser par les composants LeaderBoardComponent et QuizzResultComponent
export class QuizzResultComponent {
}
